import { LewisShopValidators } from './lewis-shop-validators';

describe('LewisShopValidators', () => {
  it('should create an instance', () => {
    expect(new LewisShopValidators()).toBeTruthy();
  });
});
